config:
  target: 'http://localhost:5000'
  phases:
    - duration: 30
      arrivalRate: 11
  defaults:
    headers:
      test: "test"

scenarios:
  - flow:
    - get:
        url: "/test/block-two"
